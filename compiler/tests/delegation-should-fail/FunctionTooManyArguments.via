/* IncorrectNumberOfArgumentsError */

fun add10(a: int{A}, b: out int{B}) {
    out b = declassify (endorse a to {alice & bob<-} from {alice}) + 10 to {(alice|bob)-> & (alice&bob)<-};
}

process main {
    val array = Array[int]{alice}(10);
    val a: int{alice} = 0;
    add10(a, val b, 20);
    output a to alice;
    output b to bob;
}

hosts: alice, bob

