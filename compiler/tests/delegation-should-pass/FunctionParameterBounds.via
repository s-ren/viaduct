fun add10(a: int, b: out int{b}) {
    out b = a + 10;
}

process main {
    val array = Array[int]{alice}(10);
    val a: int{alice} = 0;
    add10(a, val b);
    output b to alice;
}

hosts: alice, bob
